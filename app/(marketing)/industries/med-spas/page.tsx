export default function MedSpasPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-6 py-16">
      <h1 className="text-3xl font-semibold">Med Spas</h1>
      <p className="mt-3 text-zinc-600">
        High-ticket consults need fast response. ResponseOS catches the
        consult request, qualifies fit (service, timing, decision-maker),
        and books before the lead cools off.
      </p>
    </main>
  );
}
