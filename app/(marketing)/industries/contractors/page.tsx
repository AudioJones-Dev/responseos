export default function ContractorsPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-6 py-16">
      <h1 className="text-3xl font-semibold">Contractors</h1>
      <p className="mt-3 text-zinc-600">
        Remodelers, GCs, specialty trades. ResponseOS routes inbound to the
        right estimator, captures the scope, and tracks the quote-to-job
        funnel without the spreadsheet sprawl.
      </p>
    </main>
  );
}
