export default function DemoPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-6 py-16">
      <h1 className="text-3xl font-semibold">Demo</h1>
      <p className="mt-3 text-zinc-600">
        Walk through ResponseOS using mock data: a missed call lands, the
        AI follow-up triggers, the lead qualifies, the booking is created,
        the quote is sent, and the dashboard shows recovered revenue.
      </p>
      <p className="mt-4 text-sm text-zinc-500">
        Demo content is a placeholder in this local scaffold.
      </p>
    </main>
  );
}
